generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Pessoa {
  id     Int    @id @default(autoincrement())
  nome   String
  cidade String
  email  String @unique
}